# Google Gemini API key (get one at https://aistudio.google.com/apikey)
# Used by the frontend (Vite) and by the API server for story expansion.
VITE_GEMINI_API_KEY=

# For AI story expansion, run the API server so the app can call Gemini without CORS:
#   npm run server
# Or run both app and server: npm run dev:all
